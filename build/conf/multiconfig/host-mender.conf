INHERIT += "mender-full"

MENDER_STORAGE_TOTAL_SIZE_MB = "4000"
MENDER_DATA_PART_SIZE_MB = "256"

IMAGE_FILE_EXTENSION = "sdimg"

IMAGE_FSTYPES = " \
    hddimg \
    ${IMAGE_FILE_EXTENSION}.xz \
    "

MENDER_ARTIFACT_NAME = "mion-${IMAGE_NAME}-${MACHINE}-${IMAGE_VERSION_SUFFIX}"

MENDER_STORAGE_DEVICE_stordis-bf2556x-1t = "/dev/sda"

